Django==5.1
djangorestframework==3.14.0
django-tailwind==3.3.0
psycopg2==2.9.7  # Required if you're using PostgreSQL
channels==4.0.0
channels-redis==4.0.0  # If using Redis as the channel layer
django-environ==0.9.0
gunicorn==20.1.0  # WSGI HTTP server for deployment
whitenoise==6.5.0  # For serving static files in production
